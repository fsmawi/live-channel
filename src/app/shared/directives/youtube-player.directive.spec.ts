import { YoutubePlayerDirective } from './youtube-player.directive';

describe('YoutubePlayerDirective', () => {
  it('should create an instance', () => {
    const directive = new YoutubePlayerDirective();
    expect(directive).toBeTruthy();
  });
});
